version: '3.8'

services:
  db:
    image: postgres:15
    container_name: postgres_db_finapp_project
    restart: unless-stopped
    environment:
      POSTGRES_USER: finapp_user
      POSTGRES_PASSWORD: P@ssw0rd
      POSTGRES_DB: finapp_project_db
    ports:
      - "5432:5432"