version: '3.8'

services:
  db:
    image: postgres:15
    container_name: postgres_db_ddd_project
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres_user
      POSTGRES_PASSWORD: P@ssw0rd
      POSTGRES_DB: ddd_project_db
    ports:
      - "5432:5432"